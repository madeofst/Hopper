[gd_resource type="ShaderMaterial" load_steps=3 format=2]

[ext_resource path="res://Shaders/Transition/6027-251382cc.jpg" type="Texture" id=1]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform float fill : hint_range(-0.01,1.0) = -0.01;
uniform vec4 color : hint_color;
uniform sampler2D heightMap;

void fragment(){
	COLOR = texture(TEXTURE, UV);
	float h = texture(heightMap,UV).x;
	h = clamp(floor(h+fill),0.0,1.0);
	COLOR = vec4(color.rgb,color.a*h);
}"

[resource]
shader = SubResource( 1 )
shader_param/fill = -0.01
shader_param/color = Color( 0.129412, 0.168627, 0.137255, 1 )
shader_param/heightMap = ExtResource( 1 )
